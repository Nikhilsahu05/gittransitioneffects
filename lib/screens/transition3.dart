import 'package:flutter/material.dart';

class Transition3 extends StatelessWidget {
  const Transition3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Fade"),
      ),
    );
  }
}
