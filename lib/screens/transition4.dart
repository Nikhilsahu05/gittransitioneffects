import 'package:flutter/material.dart';

class Transition4 extends StatelessWidget {
  const Transition4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("LEFT TO RIGHT WITH FADE"),
      ),
    );
  }
}
